# Backend service dependencies (pinned). The omnifold_publication package
# itself is imported from the repo root (see README), not installed here.
fastapi==0.135.1
starlette==0.52.1
uvicorn==0.42.0
pydantic==2.12.5
pydantic-settings==2.14.2
python-multipart==0.0.32
prometheus-client==0.25.0

# --- optional, for production backends ---
redis==8.0.1          # cache_backend=redis

# --- test / tooling ---
httpx==0.28.1         # FastAPI TestClient
pytest==9.1.1
ruff==0.15.21
mypy==2.3.0
